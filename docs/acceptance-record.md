# 验收记录

日期：2026-08-04

本记录只列出本轮实际执行结果。所有测试凭据、设备指纹和完整卡密均保存在 Git 忽略的本机 QA 状态文件中，未写入终端、日志、快照或本文档。

## 部署一致性

- `admin-web` 已以 `--no-cache` 重新构建并启动，未删除或重建数据库卷。
- 4175 当前容器镜像：`sha256:d560bd2bebb5e178abfc0414fec0579c32dc3fb7903eaf3f1b622592cdc254ac`，镜像创建时间为 `2026-08-04T03:14:49Z`，容器健康状态为 `healthy`。
- 容器返回的脚本资源为 `index-BX5yEVfj.js`；本地构建文件与容器文件 SHA-256 均为 `1baf044fbb3484101852cf982e12cf46d3af50bf29ba2aeefb74a1bf105a402b`。
- 生产启动检查和外部冒烟均确认页面标题“逻栖工枢 · 运营中心”及“运营中心登录”“登录运营中心”“关键操作将记录审计日志”。
- 匿名进入登录页不显示会话失效；真实受保护会话失效后仅显示一次，刷新后消失。

## 授权业务闭环

独立 E2E MySQL、授权服务、后台端口及 Electron `userData` 下实际通过：

- 新手机号免短信注册、重复手机号、错误密码、正确登录但无会员。
- 错误格式卡密、不存在卡密、DAY 卡创建、客户端真实兑换、自动重启进入完整工作中枢。
- 主题、账号、授权和工作目录在重启后持久化。
- 卡密不可跨账号复用；同账号第二设备按一码一机拒绝。
- 设备封停后心跳进入受限页；恢复和解绑后按后台状态重新授权。
- 用户封停、卡密撤销、会员过期、离线宽限有效与结束均进入预期状态。
- 退出登录后核心服务停止；重新登录正确恢复。
- 网络错误、服务 500、刷新令牌失效及重复提交均有明确结果，未产生重复注册、重复兑换或重复重启。
- 桌面真实激活后自动重启通过；随后连续 10 次启动/关闭通过。共记录 11 个授权态检查点，未出现 EPIPE、`render-process-gone`、`require()` ESM 回退或 ANSI 控制字符，隔离主日志最大 219,673 字节。

## 运营中心登录后验收

验收页面全部来自 14175 隔离环境中的当前生产构建产物：

- SUPER_ADMIN：首次强制改密、运营概览、用户搜索/详情/封停/恢复、DAY/MONTH/YEAR 套餐、卡密生成/查询/撤销、设备搜索/封停/恢复/解绑、审计查询和退出登录通过。
- CSV 下载按钮已从生产页面实际触发；浏览器驱动未暴露下载事件，CSV 响应内容由同批真实 API E2E 独立验证通过。
- OPERATOR：可见运营写操作，审计入口隐藏，直接进入审计路由返回 403 无权限页。
- AUDITOR：可查询审计，写操作按钮隐藏。
- 页面刷新保持会话；真实会话失效只提示一次。

## 质量门结果

| 项目 | 实际结果 |
| --- | --- |
| 根 TypeScript | 通过 |
| Electron TypeScript | 通过 |
| 根全量 Vitest | 279 个文件通过、1 个文件跳过；2841 个测试通过、24 个跳过 |
| 根全量 ESLint | 通过；导入排序和未使用变量已做机械修正 |
| 品牌扫描 | 1104 个文件通过 |
| Windows 桌面生产构建 | 通过；生成未签名 QA 安装包，231,383,896 字节 |
| 授权服务单元测试 | 14 个通过、1 个需显式 MySQL 的入口测试跳过 |
| 授权服务真实 MySQL 集成 | 迁移通过，2 个集成测试通过 |
| 授权服务构建 | 通过 |
| 运营中心 ESLint | 通过 |
| 运营中心测试 | 12 个通过 |
| 运营中心构建 | 通过 |
| Compose 配置检查 | 生产与 E2E 配置均通过 |
| 4175 生产容器品牌/接口冒烟 | 通过 |
| 授权 API E2E | 22 组通过 |
| 隔离桌面 E2E | 真实激活、自动重启及 10 次生命周期通过 |
| SSRF 回归 | 根测试 68 个、上游 OpenClaw 156 个，共 224 个通过 |

## 日志与崩溃说明

- 主日志单文件上限已由 80 MiB 调整为 10 MiB，原有按日期命名与 7 天留存不变；本轮未删除现有日志。
- 最终检查时常规日志目录内 `main-2026-08-02.log` 为 732,425 字节，`main-2026-08-03.log` 为 858,266 字节，未发现 `.old.log`。复查描述中的约 69.41/80 MB 文件在本轮实际检查时已不存在，因此没有对其执行删除。
- 历史两次事件在旧日志中表现为同一次 `reason=killed`、退出码 `0x40010004` 被两个监听器重复记录；缺少崩溃转储，不能据此证明 GPU 是原因。本轮没有关闭 GPU，改为单点详细诊断，并在 10 次生命周期复测中未再出现渲染进程崩溃。

## 真实用户目录兼容性复测

- 2026-08-04 14:11 首次用常规用户目录启动时，发现 `tools.web.fetch.ssrfPolicy` 误用了只属于 `browser.ssrfPolicy` 的字段，当前捆绑 OpenClaw 因严格 schema 校验拒绝启动。该问题没有交给通用修复命令覆盖配置，而是在配置同步层修正。
- 修正后重新执行 68 个本项目 SSRF/配置测试及 156 个上游 OpenClaw SSRF 测试，224 个全部通过。
- 14:18 使用同一真实用户目录重新启动，OpenClaw 网关在 6,202 ms 内进入 `ready=true`，回环端口 18789 正常监听；本次启动后 `Invalid config`、EPIPE 和 `render-process-gone` 均为 0。

## 发布阻断项

- QA Windows 安装包未做 Authenticode 签名，也未在干净 Windows 虚拟机完成安装/升级验证，不可作为正式发布件。
- 本轮验证的是本机隔离 MySQL/HTTP 环境；生产域名、TLS、备份恢复和正式密钥托管仍需部署方环境完成。
- 历史渲染崩溃缺少当时的 dump，精确根因无法追溯；新诊断将在再次发生时记录可定位证据。
